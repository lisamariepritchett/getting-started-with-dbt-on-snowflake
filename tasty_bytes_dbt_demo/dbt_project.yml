## Project
name: tasty_bytes
version: "1.0"
config-version: 2
profile: tasty_bytes

## Sources
model-paths: [models]
analysis-paths: [analysis]
test-paths: [tests]
seed-paths: [seeds]
macro-paths: [macros]
snapshot-paths: [snapshots]

## Target
target-path: target
clean-targets: [target, dbt_modules]

## Models
models:
  tasty_bytes:
    staging:
      +materialized: view
      marketing:
        +schema: marketing
        +database: stage_db
      finance:
        +schema: finance
        +database: stage_db
    marts:
      +materialized: table
      marketing:
        +schema: marketing
        +database: edw_db
      finance:
        +schema: finance
        +database: edw_db
